name: window 10(BlueStacks main)

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-2025
    timeout-minutes: 350

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name "LimitBlankPasswordUse" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User (Rayat) No Password
        run: |
          if (-not (Get-LocalUser -Name "Rayat" -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name "Rayat" -NoPassword -AccountNeverExpires
          }
          Set-LocalUser -Name "Rayat" -PasswordNeverExpires $true
          $user = [ADSI]"WinNT://localhost/Rayat,user"
          $user.Put("PasswordExpired", 0)
          $user.SetInfo()
          Add-LocalGroupMember -Group "Administrators" -Member "Rayat"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "Rayat"

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.92.3-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) { exit 1 }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Install and Setup AnyDesk
        run: |
          # Download AnyDesk
          Invoke-WebRequest -Uri "https://download.anydesk.com/AnyDesk.exe" -OutFile "AnyDesk.exe"
          
          # Install AnyDesk Silently
          Start-Process .\AnyDesk.exe -ArgumentList "--install", "$env:ProgramFiles(x86)\AnyDesk", "--silent" -Wait
          
          # Set a Password for Unattended Access (Replace 'YourStrongPassword' if desired)
          echo "YourStrongPassword" | & "$env:ProgramFiles(x86)\AnyDesk\AnyDesk.exe" --set-password
          
          # Get AnyDesk ID to show in GitHub logs
          $anydeskId = & "$env:ProgramFiles(x86)\AnyDesk\AnyDesk.exe" --get-id
          echo "ANYDESK_ID=$anydeskId" >> $env:GITHUB_ENV
          
          # Launch AnyDesk in the background as the system service
          Start-Process "$env:ProgramFiles(x86)\AnyDesk\AnyDesk.exe" -WindowStyle Hidden

      - name: Verify RDP Accessibility
        run: |
          Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389

      - name: Maintain Connection + Watch RDP User
        run: |
          Write-Host "`n=== ACCESS CREDENTIALS ==="
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "AnyDesk ID: $env:ANYDESK_ID"
          Write-Host "AnyDesk Password: YourStrongPassword"
          Write-Host "User: Rayat (No Password)"
          Write-Host "==========================`n"
          while ($true) { Start-Sleep -Seconds 300 }
